version: v1.0
name: Test MyApp
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
blocks:
  - name: Tests
    task:
      prologue:
        commands:
          - sem-version php 8.0
      jobs:
        - name: Tests
          commands:
            - php --version
            - composer install
